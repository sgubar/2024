#include <stdio.h>
#include <stdlib.h>
#include "math.h"
#include <math.h>

int main()
{
int a= value (1);
int b= value (2);
int c= value (3);
if (b==0)
{
    printf (" Число 2 не може бути 0 \n");
}else{
printf (" Q = %f", q(a , b, c));

return 0;
}
}