#ifndef math_tool_h
#define math_tool_h

#include <ctype.h>
#include <string.h>
#include <stdio.h>

int add(int , int );
int sub(int , int );
int mul(int , int );
int div(int , int );
int power(int , int );
int algebra_sum_power_of_two(int , int );
int math_modulos(int );
int check_natural( );

#endif
